# PUP Online Parking Reservation — Python dependencies
# Install into a venv:  uv pip install -r requirements.txt   (or plain pip)
Django==6.0.7
django-environ==0.14.0
PyMySQL==1.2.0          # pure-Python MySQL driver (no C toolchain needed)
qrcode[pil]==8.2        # QR generation for reservation verification
Pillow==12.3.0          # image backend for qrcode
requests==2.34.2        # PayMongo REST calls
whitenoise==6.11.0      # compressed, fingerprinted static assets in production
