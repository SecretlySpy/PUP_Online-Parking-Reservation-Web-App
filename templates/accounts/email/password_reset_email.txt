Hello{% if user.get_full_name %} {{ user.get_full_name }}{% endif %},

We received a request to reset the password for your PUP Online Parking
Reservation account ({{ user.get_username }}).

Use the one-time link below to choose a new password. It can only be used once
and expires shortly:

{{ protocol }}://{{ domain }}{% url 'accounts:password_reset_confirm' uidb64=uid token=token %}

If you didn't request this, you can safely ignore this email — your password
will stay the same.

— {{ site_name }}
